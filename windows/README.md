# Windows setup

1. Run the powershell script [install list](winget-install-list.ps1).

2. Add the [powershell profile](powershell_profile.ps1) to the PS home repository (`$HOME\Documents\PowerShell\Profile.ps1`).

3. Install the fonts.
